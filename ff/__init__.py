"""Fire Forex foundation package.

Provides schema primitives (schema), signal-family registry (signal_lib),
schema-to-Rust encoder (encoding), random sampler (sampler), pre-flight
reporter (preflight) and run harness (harness).

EAs live under ``eas/`` and are loaded by ``run.py``.
"""
