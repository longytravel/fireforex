# Fire Forex local web UI dependencies.
# Everything else (numpy, pandas, backtester_core) already lives in .venv
# and is managed by the existing Fire Forex runtime setup.
fastapi>=0.136.1
uvicorn[standard]>=0.29
pyyaml>=6.0.3
pytest>=9.0.3
httpx>=0.28.1
dukascopy-python>=4.0.1

