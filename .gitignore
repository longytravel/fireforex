# Rust build artefacts
target/
*.pyd
*.so

# Python
__pycache__/
*.pyc
.pytest_cache/
.venv/

# Run artefacts (keep history.csv + baseline.json; drop heavy npz)
artifacts/runs/
artifacts/signal_cache/
artifacts/comparison.html
artifacts/volatility_cache.json
artifacts/data_inventory.json

# Playwright cache
.playwright-mcp/

# Editor / OS
.idea/
.vscode/
.claude/
Thumbs.db
.DS_Store

# Local dev scaffolding
artifacts/_kill.ps1
artifacts/server.log
artifacts/server.err

# Live-parity runner — VPS-only creds + ephemeral runtime state.
# Never commit these: they contain MT5 credentials and ticket/plan logs.
.env.live
.env.live.*
artifacts/live/
artifacts/calibration/
artifacts/parity/
artifacts/replay/

# Transient laptop-side staging for VPS artifact sync + one-off diagnostics.
artifacts/_incoming/
artifacts/live_archive_*/
artifacts/_reconcile_rerun_*.log
artifacts/diag_*.txt
scripts/_tmp_*.py
